import { Redirect } from 'expo-router';

export default function Index() {
  // The redirection logic is handled in AuthContext.tsx
  // This file exists to ensure the root route is matched.
  return null;
}
